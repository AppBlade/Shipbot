<%= simple_form_for(@provisioning_profile, :multipart => true) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :mobileprovision, :as => :file %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
