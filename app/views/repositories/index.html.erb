<% content_for :title do %>
  Repositories
<% end %>

<p>Tracked repositories will be scanned for Xcode Projects, build targets, and monitored for changes.</p>
<p>Manual builds can be fired off from the <%= link_to 'projects page', :xcode_projects %>, for an automatic build just push a tag.</p>

<table class="table table-striped table-bordered">

  <tr>
    <th>Full name</th>
    <th colspan="2"></th>
  </tr>

  <%= render @repositories %>

</table>
