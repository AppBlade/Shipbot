<% content_for :title do %>
  Edit <%= @native_target %> build rule
<% end %>

<%= render 'form' %>

<%= link_to [:new, @native_target, @build_rule], :class => 'btn btn-large' do %>
  <i class="icon-plus"></i> Or add another build rule
<% end %>
